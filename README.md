# WildCritic case study

This interactive case study compares an LLM critic with later human feedback on real agent responses

[View the case study](https://wild-critic.github.io/).

## Run locally

Start a local server from the repository root:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
